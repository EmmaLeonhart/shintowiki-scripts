# sequential_misc.txt — the sequential miscellaneous edits file
# =============================================================
# Emma 2026-07-10 (Open questions): "a single sequential miscellaneous file that
# is executed one by one in a random place during the 300 daily edits. It
# essentially just goes one a day."
#
# HOW IT RUNS (direct_daily_edits.py):
#   * Exactly ONE line from this file is executed per daily run, in strict
#     top-to-bottom order, woven in at a random position among the day's edits.
#   * A cursor (sequential_misc.state) records the next line. It advances only
#     when today's line reaches its end state (a successful edit, or a removal
#     whose target is already gone); it HOLDS on any error / rate-limit / freshness
#     skip, so the line retries next run.
#   * Because line N is confirmed landed before line N+1 is ever attempted, this
#     is the ONE safe home for remove-then-add and add-then-remove PAIRS that the
#     random atomic drip cannot run safely (the remove could otherwise fire first
#     and blank a shrine).
#
# RULES:
#   * APPEND ONLY below the cursor. Never insert or reorder an executable line
#     above lines already run — the cursor is an index into the executable
#     (non-comment, non-blank) lines and would misalign. Comments/blanks may go
#     anywhere; they are filtered out before indexing.
#   * Write a PAIR in the order it must run: the line that must land first ABOVE
#     the line that depends on it. One edit per line (no "||" compounds here).
#   * QuickStatements v1 syntax, same as the atomic files. A leading "-" is a
#     removal.
#
# ---------------------------------------------------------------------------
# Emma's inbound-links fix (2026-07-17). MOVED HERE from the atomic drip.
#
# WHY IT MOVED: the atomic drip is `random.sample(all_lines, 300)` over a
# ~105,000-line pool, so a 2-line file had a ~0.28%/day chance of being drawn —
# an expected wait of roughly a YEAR. That is fine for a 400-line backlog and
# useless for a 2-line fix Emma actually wants landed. This channel runs one
# line/day in strict order, so these land on the next two runs.
#
# WHAT THEY DO: her papers already carry P50 -> Q140568964, the PRE-MERGE
# REDIRECT. WDQS does not traverse a redirect in statement position, so
# `?w wdt:P50 wd:Q140568870` returns 0 and every notability query (including
# anything run at RfD) sees an author item with no papers. Verified still 0 on
# 2026-07-17. These take her from 0 inbound papers to 2.
#
# ADD-ONLY, as Emma approved. The stale P50 -> Q140568964 statements are NOT
# removed here; that pass is now trivially safe to sequence below these two
# whenever she wants it, since the cursor confirms a line landed before moving on.
Q140568717|P50|Q140568870|P1545|"1"
Q140568719|P50|Q140568870|P1545|"1"

# ── 調田坐一事尼古神社 (Q22119431): the P1814 is a wrong name too ──────────────
# Emma 2026-08-04: "replacing all of the wrong names on 'hikida Shrine' not just
# the english one". The labels are handled in multilingual_label_fixes.txt, but
# the kana is wrong as well: the item carries くだにますひとことねこじんじゃ, which is
# the jawiki lead's つくだにますひとことねこじんじゃ with its first character missing.
#
# This is an ADD-then-REMOVE pair on the SAME property, so it cannot go in the
# atomic drip — under the random run order the removal could fire first and leave
# the shrine with no modern reading at all. This file is the one place where line
# N is confirmed landed before N+1 is attempted, which is what makes it safe.
#
# The item's OTHER P1814, ツキタノ- , is deliberately left alone: it is the ancient
# Engishiki katakana reading (plus a trailing-hyphen artifact), and relocating
# those onto an ojp-hani P1448 qualifier is the kana-qualifier cleanup's job, not
# this one. Two passes touching the same property is how values get lost.
Q22119431|P1814|"つくだにますひとことねこじんじゃ"|S143|Q177837|S4656|"https://ja.wikipedia.org/wiki/%E8%AA%BF%E7%94%B0%E5%9D%90%E4%B8%80%E4%BA%8B%E5%B0%BC%E5%8F%A4%E7%A5%9E%E7%A4%BE"
-Q22119431|P1814|"くだにますひとことねこじんじゃ"

# ── The ordinal-less `part of` leftovers (2026-09-18) ───────────────────────────
# Generator: generate_p361_leftover_removals.py (re-runnable, read-only against
# Wikidata). Population: the `blank_ordinal_side` class of
# p361_multi_part_of_audit.json — a shrine holding TWO P361 statements into the
# same Engishiki list, one carrying the P1545 ordinal the list names and one
# carrying nothing. The blank one is import residue.
#
# WHY HERE AND NOT IN AN ATOMIC FILE. Both statements carry the same value, so
# `-Q|P361|Qlist` does not say which to take; what decides is
# direct_daily_edits.find_claim, which removes the FIRST matching claim and never
# looks at qualifiers. Each line below was emitted only because the blank
# statement is live claim [0] and an ordinalled one survives it. Drawn a SECOND
# time the same line would match that survivor and take the membership — so the
# line must run exactly once, which is what this file's cursor guarantees and the
# random atomic pool does not.
#
# NOT a pair: each line stands alone, no add follows.
#
# The 14 `true_duplicate` items are deliberately absent. All 14 are already staged
# in list_membership_removals.txt / multi_ordinal_removals.txt /
# orphan_membership_removals.txt, which strip an affected item's membership into
# that list entirely — Emma 2026-08-25, "we remove it and then we add it again."
# A line here would be one removal past that, aimed at whatever survives the drip.
#
# Q107306769 網野神社 is also absent: its blank side has since acquired ordinal 44
# and now sits at claim [0] carrying two references, so value-matching reaches the
# better of the two statements and not the leftover. The 2026-07-10 ruling on that
# shape stands — report only.
-Q107664337|P361|Q11368516
-Q112953068|P361|Q11668489
-Q11383014|P361|Q11658590
-Q11393842|P361|Q11465292
-Q11487792|P361|Q11553385
-Q11607305|P361|Q11467693
-Q123118271|P361|Q11458212

# ── P958 section corrections (2026-09-18) ──────────────────────────────────────
# Generator: generate_p958_corrections.py (re-runnable, read-only against Wikidata).
#
# A P13677 Kokugakuin id is unique by the PAIR (id, P958 section), so an item whose
# section is wrong is asserting it is a different numbered candidate than it is.
# generate_p958_qualifiers.py is add-only and cannot reach a statement that already
# carries a P958, so these three are invisible to it forever.
#
# WHY HERE AND NOT IN AN ATOMIC FILE. QuickStatements has no verb that overwrites a
# qualifier — Help:QuickStatements lists "remove a qualifier without removing the
# statement itself" under what it cannot do — and the shape that LOOKS like one,
# `-Q|P13677|"id"|P958|"old"`, deletes the whole statement. That is the shape
# direct_daily_edits.execute_removal refuses, after four items lost their entire
# ojp-hani P1448 name to it on 2026-09-09. So a correction is a genuine
# remove-then-REBUILD: the '-' line takes the statement and the line below it
# recreates it with the right section plus every qualifier and reference the live
# statement carried. Only this file guarantees the rebuild follows its removal.
#
# Each rebuild was generated from the live statement on 2026-09-18. All three are
# rank normal and carry no references; the two 尾津/東大谷 items carry P3831 =
# Q135159299, which the rebuild restores.
#
# Q134925373 181621 'n/a' -> '0'; Q135186791 181329 '1' -> '2' (it is labelled 尾津神社
# exactly and the page numbers that candidate (2)); Q135069120 180834 '1' -> '2' (the
# page lists exactly one candidate and numbers it (2), so there is no slot 1 at all).
#
# The fourth correction, Q111776816 181621 -> '1', is NOT here: its statement carries no
# P958 at all, so it is an ordinary add and drips from p958_corrections.txt.
-Q134925373|P13677|"181621"
Q134925373|P13677|"181621"|P958|"0"
-Q135186791|P13677|"181329"
Q135186791|P13677|"181329"|P958|"2"|P3831|Q135159299
-Q135069120|P13677|"180834"
Q135069120|P13677|"180834"|P958|"2"|P3831|Q135159299
