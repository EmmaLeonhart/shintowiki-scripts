
__pycache__/
*.pyc
*.pyo
.env
tmpclaude-*/
.cleanvibe-scratch/
desktop.ini
.claude/scheduled_tasks.lock
shinto_miraheze/manual_import_local.py
# _site/ is tracked — committed locally and updated by CI

# Saved claude.ai session pages (raw) — keep only the cleaned extract in docs/session_logs/
/Claude Code.html
/Claude Code_files/
/_session_log_extracted.txt
/shinto_miraheze/*.out.json

# cdo corpus fetch log (temp)
shinto-label-generator/cdo_corpus_fetch.log
.gstack/

# Historical province polygons (CODH, CC BY-NC). Cached locally, never committed
# and never republished — only the derived point-in-polygon fact leaves the box.
# See docs/province_shapefiles.md.
modern-quickstatements/.province_cache/
modern-quickstatements/.saijin_research_run.log
_site/empty-items-export.xml

# jinjacho crawl progress log (regenerated each run; the data lands in crawled_shrines.csv)
jinjacho/crawl.log
jinjacho/crawl_fast.log
jinjacho/crawl_gifu.log
jinjacho/match_run*.log
# local test residue: direct_daily_edits tests write this cursor (untracked in f2ca841e8)
modern-quickstatements/sequential_misc.state
modern-quickstatements/shinmei_run.log

# transient crawl/run logs (jinjacho background chunks)
jinjacho/*_chunk.log
jinjacho/match_run*.log

# UA contact addresses — the real values are repo secrets (MIRAHEZE_UA_CONTACT / WIKIDATA_UA_CONTACT)
shinto_miraheze/.ua_contacts.json

