__NOTOC__

== Open questions ==

''These are the things we reported on over 2026-07-09/10 that are waiting on you. Each links a full write-up in <code>docs/</code>. Strike or answer inline and the bot deletes the bullet once acted on.''

'''Engishiki lists — decisions you deferred ("report only, decide later")'''
* '''150 confirmed Shikinaisha no list names.''' 84 are the same shrine twice — a living-shrine item and the separate 927-entry item the list already names (matched by shared Kokugakuin id, exact name, or old-vs-new spelling). 66 have no twin at all. Two questions: link/merge the 84 pairs, and are the 66 mis-tagged modern shrines or entries the lists are missing? (<code>docs/orphan_shikinaisha_2026-07.md</code>)
* '''Izumo names one item for two entries.''' 坐韓国伊大弖神社 covers both 意宇郡 entry 28 (in 揖夜神社's grounds) and entry 39 (伊'''太'''弖, in 佐久多神社's). Ordinal 39 is an empty hole. Splitting it needs a '''new item''' — not something the QuickStatements pipeline can make. (<code>docs/engishiki_list_defects_2026-07.md §1</code>)
* '''The 17 palace kami.''' The Imperial Palace list names four grouped items (八神殿, 座摩神, 御門巫祭神 八座, 生島巫祭神 二座) but you also hold an item for each of the 17 individual deities (Kokugakuin 180542–180558). Both models say true things; which is canonical? (<code>docs/engishiki_list_structure_2026-07.md</code>)
* '''22 duplicated "part of" statements''' on named parts (the old import damage; 天王宮… holds three). QuickStatements removes by value, so it cannot delete one identical twin without risking the other. Only a browser remove-and-re-add fixes these. (<code>docs/ronsha_list_membership_2026-07.md</code>)

'''Hand edits only you can make (the add half is already queued in the drip)'''
* '''Awa list:''' delete the "has part → 下立松原神社" statement at ordinal '''3''' on 安房国の式内社一覧. Entry 3 is 天神社; a piped link was imported over it. The add of 天神社 at ordinal 3 is queued. Value-shared with the correct ordinal-5 statement, so the delete cannot be a QuickStatement. (<code>docs/engishiki_list_defects_2026-07.md §1</code>)

'''Batches waiting for you to run them by hand''' (remove-only, unregistered, each gated on its add having already landed)
* '''List membership:''' 2,236 removals stripping the list link from the 2,151 Ronsha no list names. (<code>generate_list_membership_removals.py</code>)
* '''Province exclusions:''' script 2, after the drip works through the two corrections. (<code>generate_province_exclusion_removals.py</code>)
* '''Takano address merge:''' script 2, once the merged 〒708-0013 address lands. (<code>generate_ronsha_address_merge_removals.py</code>)

'''Needs you to explain what it is'''
* '''Kokugakuin id matcher''' — you asked to explain this task before it is decided. (<code>match_kokugakuin_ids.py</code>, <code>kokugakuin_id_report.txt</code>)

== Wiki-based queue ==

''(empty — items are picked up into queue.md and removed from here)''

== Notes ==

''(free-form scratch space for Emma)''

[[Category:Git synced pages]]