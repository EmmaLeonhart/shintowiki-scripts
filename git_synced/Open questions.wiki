__NOTOC__

== Open questions ==

Claude are the eight things resolved or not? This was a thing that we made in the handoff about a month ago, it feels like. I don't know the degree to which it exists anymore, but please list all of them below this, all of them in a response to this comment, and whether they are resolved or not. [[User:Immanuelle|Immanuelle]] ([[User talk:Immanuelle|talk]]) 20:55, 7 July 2026 (UTC)

: The eight things are the backlog-board items (<code>docs/backlog_resolution_status_2026-07-05.md</code>, written 2026-07-05 — two days ago). Status as of 2026-07-07, updated for everything since:
:# '''retire-terminating-scripts — RESOLVED.''' All four July-gated scripts confirmed inert and deleted; dead launchers removed.
:# '''audit-legacy-scripts — RESOLVED.''' Keep/fix/retire verdicts in <code>docs/program_audit_2026-06.md</code>; no silently-inert CI steps remain.
:# '''ill-missing-wikidata — RESOLVED (automation).''' Detection op + <code>fix_ill_destinations.py</code> run in every cleanup fire; the only residual is the deliberately-skipped low-confidence ills, which are per-page human review by design.
:# '''duplicate-qid-tail — RESOLVED (≈7-page remnant).''' Down from ~621 to ~7 genuinely-different-target dab pages; those are inherent human review, everything else auto-drains.
:## show me the items I need to make decisions on, do not just say it requires human review and not show them to me
:# '''japanese-category-names — AUTOMATED, still draining.''' The one item that is not "done", but there is no build work left: deterministic resolvers shipped, the whole residual routes through cloud RAG → <code>category_moves.csv</code> → monthly <code>move_categories</code> (378 work-files pending; the collector bug that silently blocked answers was found and fixed today, 2 answers folded). ~1,186 Japanese-named categories remain in the pipeline's stomach.
:# '''multiple-wikidata-links — RESOLVED (automation).''' Op + report page run in CI, self-healing; residual is pick-the-right-QID human review per case.
:# '''duplicated-content-need-translation — RESOLVED (automation).''' The cloud routine works it (116 dup-content + 363 need-translation pending); manual only for canonical-title/history-merge hard cases and the 9 kokuzō articles.
:# '''recreate-deleted-wikidata — RESOLVED BY DECISION (2026-07-06).''' The ill-referenced deleted targets were recreated (you ran the QS) and relinked; the ~213 unreferenced deleted items are deliberately NOT recreated per your call. Nothing open.
: Net: nothing on the board has open build work. #5 is the only one still churning, and it churns autonomously.

== Wiki-based queue ==

''(empty — items are picked up into queue.md and removed from here)''



== Notes ==

''(free-form scratch space for Emma)''

[[Category:Git synced pages]]