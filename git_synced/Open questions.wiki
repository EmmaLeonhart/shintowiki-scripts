__NOTOC__

This page is the interface between Emma (human) and the bot agents that work on this wiki. Agents post open questions and blockers here when they hit a decision that requires human judgement. Emma answers them on the wiki. Agents read the wiki version each session and act on the answers.

== How to use this page ==

* '''Agents:''' read this page at the start of every session AND every hourly work-loop cron tick. When you hit a blocker, add it under the right urgency section with enough context that Emma can answer without re-deriving the problem. '''When you verify an item is actually resolved (via code/API/log inspection), DELETE the bullet in the same commit''' — no "recently resolved" graveyard; history lives in <code>DEVLOG.md</code> + git log. Investigate before declaring something blocked-on-Emma; many "blockers" are autonomous work that just needs starting.
* '''Emma:''' edit answers in-line under each question; delete it once acted on. Free-form context goes under [[#Notes|Notes]].
* '''This page is wiki-wins.''' The wiki is canonical. Edits Emma makes here on shinto.miraheze.org propagate down to the repo. Agents must not overwrite Emma's wiki edits with stale repo content. (A bot clobber of this page happened 2026-06-07 and is being fixed — see the sync-bug item below.)

== Decisions needed — HIGHER URGENCY ==

'''A. "Pages without wikidata legacy" — do you want the category split?''' (raised by Emma's 2026-06-07 edit to this page)
: Right now there is ONE flat <code>[[Category:Pages without wikidata]]</code> (~403 pages), fed two ways: ~344 old pages still on the literal tag (being drained by <code>remove_crud_categories</code>; the category is marked crud until 2026-12-06), and ~58 via the blank-<code>{{wikidata link}}</code> self-categorization. Emma's edit changed a note to say "Pages without wikidata '''legacy'''".
: '''Question:''' do you want the legacy literal-tag population moved into a separate <code>[[Category:Pages without wikidata legacy]]</code> (so the template-driven category is clean), or was "legacy" just a clarifying word? If you want the split, the agent wires it into the <code>wikidata_link</code> op. '''Default if unanswered:''' treat as a clarifying note, no split.

'''B. The 9 QID-overlap MERGE cases — proceed automatically?''' (interlanguage-resolution op, in flight)
: 9 of the resolved pages share a QID with another existing shinto page (duplicates): <code>Kehi Jingū</code>↔<code>Kehi Shrine</code> (Q11129346), <code>Izanagi Jingu</code>↔<code>Izanagi Jingū</code> (Q10884977), and 7 deity/kuni-no-miyatsuko pairs (full list in <code>queue.md</code>).
: '''Question / default:''' agent will MERGE these — redirect the duplicate to the canonical page, '''keeping the "Jingū" / proper-form title and redirecting the romanised "…Shrine" variant''' (Emma's rule) — unless you say otherwise. This is a miraheze edit (via git sync), not a Wikidata edit, so it proceeds under the "everything except Wikidata" go-ahead.

== Lower urgency / maybe ==

'''C. The 304 "deleted/missing QID" link targets — currently NO-GO.''' (reframed 2026-06-07)
: <code>[[Category:Pages with deleted QID in ill template]]</code> = 145 pages containing <code>{{ill|…|qid=Qxxx}}</code> whose Qxxx now returns "missing" from Wikidata (304 distinct targets). Sampled one — <code>Q135490857</code> was deleted 2026-05-19 as an "Empty item" (created empty by the import, then deleted), not for the subject being non-notable. Targets split into historical people (notable) and many small local shrines (riskier).
: '''Status: parked.''' Recreating these means CREATING Wikidata items, which Emma has ruled a hard NO ("suicidal", 2026-06-07). So: leave <code>qid=DELETED_QID</code> in place, surface the set on the dashboard as "needs a real article first". Revisit only if the no-Wikidata-creation policy changes.

== Backlog status — the 8 todo.md items (verified live 2026-06-07) ==

''Some verify instantly (category count); some lag because they drain over many throttled orchestrator cycles. Resolved ones get deleted from todo.md, not parked.''
# '''Retire terminating cleanup scripts''' — TIME-GATED to July 2026; not actionable yet.
# '''Audit legacy scripts''' — DONE (catalogued in <code>docs/program_audit_2026-06.md</code>); only the empirical "still producing edits?" check on the July-gated scripts remains (overlaps #1).
# '''ILLs without WD= / "Unknown"''' — ONGOING, 1000+ in <code>[[Category:Pages with unresolved QID in ill template]]</code>; orchestrator-fed, drains/grows over cycles, not a one-shot.
# '''Double category qids''' — NEARLY DONE: down to a '''4-page''' tail; <code>resolve_double_category_qids</code> auto-handles same-target cases.
# '''Japanese-language category names''' — ONGOING, '''1171 subcategories''' — the big real backlog; Wikidata-anchored resolver shipped and draining.
# '''Multiple <code>{{wikidata link}}</code> per page''' — CURRENTLY CLEAR (0); self-maintaining monitor op, may repopulate.
# '''Duplicated content + need-translation''' — ONGOING: 134 dup-content + 385 need-translation; worked by the cloud-queue routine.
# '''Recreate deleted Wikidata items''' — see decision C (NO-GO, parked).

== Open agent task: fix the git-synced clobber bug ==

The 2026-06-07 clobber of THIS page happened because the sync's "most-recent-edit-wins" effectively compared repo state against the wiki in a way that let unrelated repo commits look "newer" than Emma's wiki edit, so it overwrote her edit. Agent to-do (non-Wikidata, in progress): make the comparison strictly per-file (last commit touching THAT file vs that page's top wiki revision), and/or add a hard wiki-wins guard for this page. Tracked in <code>queue.md</code>.

== Done this session (2026-06-07) — FYI, agent deletes once Emma has seen it ==

* Repo homepage/description set to the GitHub Pages dashboard.
* Dashboard: surfaced all 8 backlog items on the About page; fixed the dead "Linked to Wikidata" stat and corrected the 403 explanation.
* Fandom <code>{{ill}}</code> fixed (dead interwiki links → external <code>https://</code> URLs; per-wiki template split). Verified live.
* Interlanguage-resolution op: 57 pages pulled to <code>git_synced/</code>, 18 QIDs resolved & filled.
* Clobber of this page restored ("legacy" edit is back on the wiki).

== Notes ==

''(free-form scratch space for Emma)''

== Sync-policy exception note ==

Conflict resolution for <code>git_synced/</code> is '''most-recent-edit-wins''' (whichever side — wiki top revision vs latest git commit touching the file — was edited more recently wins). This page is edited primarily ON the wiki by Emma. Agents: always pull this page from the wiki at session start; never push a stale repo copy; before editing it repo-side, confirm the wiki hasn't been updated since the last sync.

[[Category:Git synced pages]]
