__NOTOC__

== Open questions ==

''(Labels-pipeline report, requested 2026-07-08; clears after a week per the reports rule.)''

'''How a label gets onto Wikidata today — the whole pipeline:'''
# '''Sources of English labels''' (all end up as atomic QS files in <code>modern-quickstatements/</code>): the shinto-wiki page names (<code>fetch_en_labels_from_wiki.py</code> — [[QuickStatements/en labels]] wiki page is the hand-curated source); kana-derived romanizations (<code>kana_en_labels</code>, <code>identical_name_en_labels</code>, temple variants); the daily claude.ai Sonnet routine translating batches of shrines/temples picked by <code>select_shrines_to_translate.py</code> (<code>en_labels_sonnet</code>); and RAG-corrected typo fixes (<code>label_typo_fixes</code>, from the 161-item review queue).
# '''The 70-language machinery''' (<code>shinto-label-generator/</code>, <code>language_registry.py</code> is the single source of truth for which language uses which method): transliteration from the English/romaji form for most languages; Chinese via kana→man'yōgana + OpenCC for the zh family; Korean via koreanize (Japan) / hanja; Indonesian "Kuil X" proposals; Toki Pona; plus specialized generators for kami, Buddhist deities, misc Shinto terms, concepts (hand-authored), properties (hand-authored), court ranks, provinces, texts. Every output line carries a provenance comment. ~2.6M proposed labels currently sit in <code>quickstatements/&lt;lang&gt;.txt</code>.
# '''The drip:''' <code>select_label_proposals.py</code> feeds 20 proposals per CI run into <code>label_proposals_drip.txt</code>; everything flows to Wikidata through the single daily editor (<code>direct_daily_edits.py</code>, 300/day, random interleave).
# '''The uniqueness interlock (new this week):''' a label can only land if the label+description pair is unique — stale descriptions block label adds, so the description program runs alongside: desc-then-label pairs (100/day), class-true description adds (50/day until 2027-01), and collision groups routed to cloud enrichment for unique English descriptions (236 work-files live).
# '''Guards:''' label-generator outputs never overwrite existing labels (non-destructive checks); label-typo review corrects the legacy romaji typos; the edits-today tile on the dashboard counts the account's total daily Wikidata edits.

== Wiki-based queue ==

''(empty — items are picked up into queue.md and removed from here)''

* Anomaly corrections
** I am finally trying to systematically look over these.
** So first of all, I don't know why it is that our shrine ranking anomalies https://emmaleonhart.github.io/shintowiki-scripts/shrine-ranking.html This page is consistently not updating, which is really annoying when we're actually trying to fix this stuff! There's some sort of caching or something that's causing a problem. And I think it's possible that you may have disabled it because your earlier system of querying was some weird thing that queried everything individually. 
** Now for my analysis. I think that the P361 properties for the lists are actually pretty simple and easy to do, because all of the before and after qualifiers and the part-of qualifiers are already more or less resolved. They're resolved only on the Shikin Aisha list items; they're not resolved on these other things. Some of these get conflated because of that, so I just want you to go through all of those ones and remove every single part of the Shikin Aisha list that is on the actual shrine itself. Add in a new one that's just derived from the list item, with the preceding and following taken from the data thing. It is going to give two citations:
*** to the items like Koku Gakuin ID, which a large amount of things are already cited to
*** the different form of itIf you don't know it before, look it up, because every single one of those items that has a coordinate has that form, and also you have the Wikipedia ID. You have the Shikin Aisha list Wikipedia article, so this is super easy. You can do it.
** Honestly, for the official name thing, I want you to do an analysis of the official names. I really just want you to make an actual table on that page, because I'm pretty sure that the table of whatever the Japanese label of it is is the two official names and all of the Koku Gakuin University Shrine Database entries, with clickable links. I think that's just going to solve it for me! Every single one of them is probably going to be manually fixable for me if I just have that information.
** Street address is much more complicated. But I would say trying 
** Oh, and for the street address thing, I would, in particular for those ones, I think a predictor that I've actually seen is: if any one of them is cited, if any one of them has a citation, then that's a really good sign. Show all of the addresses with all the citations as references, like footnotes or an expanding thing or whatever.
** Yeah, so the part of duplicates is mostly just a matter of, honestly, the Shiki Naisha list stuff is pretty easily solved. It's almost all solved. It's just not solved on the shrine items. The official name stuff, I think, is easily solvable moat, but it kind of requires my intuition, and there's only a hundred and five of them. Maybe even less, because this page is stale.
** Street Address has 252 items, probably less. I'm just going to need the more sophisticated thing showing all their different addresses, the sources, and stuff first, and then I'll be able to try, and then I'll be able to figure it out better.
** And also Takagi Shrine no longer has any of those issues, so that's just a stale example.
** And remembering that for the Shikin Aisha lists we cite the Wikipedia article for the Shikin Aisha list. We cite that, although in this case citing the Wikipedia article for the Shikin Aisha list means, like, the Wikipedia, whatever. We cite the Wikipedia article for the Shikin Aisha list for the street addresses. We aren't doing any immediate citation, and I think the official names are mostly going to already have citations.

== Notes ==

''(free-form scratch space for Emma)''

[[Category:Git synced pages]]