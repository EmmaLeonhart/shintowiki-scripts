__NOTOC__

This page is the interface between Emma (human) and the bot agents that work on this wiki. Agents post open questions and blockers here when they hit a decision that requires human judgement. Emma answers them on the wiki. Agents read the wiki version each session and act on the answers.

== How to use this page ==

* '''Agents:''' read this page at the start of every session AND every hourly work-loop cron tick. When you hit a blocker (a design call, a credential need, a wiki-side action you cannot take, an ambiguous instruction), add it under the relevant section below with enough context that Emma can answer without re-deriving the problem. '''When you verify an item has actually been resolved (via code/API/log inspection), delete the bullet entirely in the same commit''' — leaving stale "open questions" lying around is a failure mode Emma has flagged. Investigate before declaring something blocked-on-Emma; many items framed as blockers are either already resolved or are autonomous work that just needs to be started.
* '''Emma:''' edit answers in-line under each question. When you've answered a question and the agents have acted on it, strike or delete the question. Free-form notes that don't fit anywhere else go under [[#Notes|Notes]] below.
* '''This page is different from other git-synced pages.''' The wiki is the canonical source of truth here. When Emma edits this page on shinto.miraheze.org, those edits propagate down to the repo (wiki-wins for this page). Agents must not silently overwrite Emma's wiki edits with stale repo content.
* '''No "Recently resolved" graveyard.''' Resolved items are DELETED immediately, not parked here to bloat the page. History lives in <code>DEVLOG.md</code> + git log. (Emma, 2026-06-07.)

== Decisions Emma needs to make ==

'''1. The 304 "deleted/missing QID" link targets — recreate which, if any?''' (the only pre-existing item; reframed 2026-06-07 with real data)
: <code>[[Category:Pages with deleted QID in ill template]]</code> = '''145 pages''' that contain an <code>{{ill|…|qid=Qxxx}}</code> whose Qxxx now returns "missing" from Wikidata. These are LINK TARGETS inside the pages, not the pages themselves. There are '''304 distinct''' such targets across the 145 pages.
: '''What "missing" means (checked 2026-06-07):''' the <code>deleted_qids_in_ill</code> op writes <code>qid=DELETED_QID</code> only when a specific Qxxx that was already written in the ill now returns "missing". Sampled one — <code>Q135490857</code> was genuinely '''deleted''' on 2026-05-19 by user MisterSynergy with reason "Empty item: content was empty". So these were created (likely by the import, as empty stubs), then deleted for being empty — NOT necessarily for the subject being non-notable. Recreating them WITH real claims (label + description + P31 + P11250) would make them non-empty, which is exactly what was missing.
: '''The targets split sharply by type:''' (a) historical people — e.g. <code>Abe Arimori</code>, <code>Abe Yasukichi</code>, the Abe/Adachi onmyōji genealogies — generally notable, safe; (b) '''many small local shrines''' — a long run of <code>Akagi Shrine (Araguchi Town)</code>, <code>Akagi Shrine (Arai Town)</code>, … individual tiny branch shrines, the riskier set.
: '''Questions for Emma:'''
:# Recreate at all? If yes, '''which subset''' — historical people only (safe default), or shrines too?
:# Minimum claim set so they survive: <code>Len</code> + <code>Den</code> + <code>P31</code> (per-target instance-of) + <code>P11250</code> (shinto: back-link). Enough?
:# This goes through the QuickStatements pipeline (the only allowed Wikidata channel). Agent default if unanswered: build the generator for the '''historical-person targets only''', skip the small shrines.

'''2. "Pages without wikidata legacy" — what do you want here?''' (raised by Emma's 2026-06-07 edit)
: Emma edited this page to change "Pages without wikidata" → "Pages without wikidata '''legacy'''" in a note about the self-categorizing <code>{{wikidata link}}</code>. Current reality: there is ONE flat <code>[[Category:Pages without wikidata]]</code> (403 pages) fed by two mechanisms — (a) ~344 pages on the OLD literal-tag, being drained by <code>remove_crud_categories</code> (the category is marked <code>[[Category:Crud categories]]</code>; crud designation lifts 2026-12-06); (b) ~58 pages via the blank-template self-categorization.
: '''Question:''' do you want the legacy literal-tag population split into a separate <code>[[Category:Pages without wikidata legacy]]</code> (distinct from the template-driven one), or was "legacy" just a clarifying note? If you want the split, say so and the agent will wire it into the <code>wikidata_link</code> op.

'''3. Interlanguage-resolution operation — proceed with the 9 merges?''' (in flight, 2026-06-07)
: The 57 "<code>{{wikidata link}}</code> present but unresolved" pages were pulled into <code>git_synced/</code> and tagged <code>[[Category:Pages git synced to resolve interlanguage and interwiki links]]</code>. 18 QIDs resolved & filled so far (14 exact-label + 4 verified search-hits). '''9 are QID-overlap MERGE cases''' — the resolved QID is already claimed by another shinto page (duplicate pages): e.g. <code>Kehi Jingū</code>↔<code>Kehi Shrine</code>, <code>Izanagi Jingu</code>↔<code>Izanagi Jingū</code>, plus 7 deity/kuni-no-miyatsuko duplicates.
: '''Question:''' proceed to merge these automatically (redirect one → the other, '''keeping the "Jingū" / proper title and redirecting the "…Shrine" / romanised variant''', per your rule)? Agent default if unanswered: do the merges, "Jingū"/native title wins, on a per-page basis. (26 of the 57 have no Wikidata item at all — those overlap with decision #1.)

== Done this session (2026-06-07) — FYI, no decision needed ==

''(Context for the above, so the decisions make sense. Agent will delete this section once Emma has seen it.)''
* '''Repo homepage + description''' set to the GitHub Pages dashboard.
* '''Dashboard fixes:''' surfaced all 8 backlog items at the bottom of the About page; replaced the dead "Linked to Wikidata" stat (the category no longer exists → showed 0) and corrected the Wikidata-count explanation (the 403 is mostly an actively-draining migration backlog, not an unresolvable tail).
* '''Fandom <code>{{ill}}</code> fixed:''' on shinto.fandom.com the interlanguage links were dead (interwiki prefixes <code>[[d:…]]</code> / <code>[[:ja:…]]</code> don't resolve there). Split <code>Template:Interlanguage link</code> per-wiki (<code>fandom_unique/</code> uses external <code>https://</code> URLs like <code>{{wikidata link}}</code> does; <code>miraheze_unique/</code> keeps interwiki). Verified live.
* '''Clobber I caused + fixed:''' my dispatched Git Synced Sync overwrote Emma's 19:04 wiki edit to THIS page (the "legacy" change). Restored it (commit 953b8746) and re-synced. Root cause worth noting: the sync's "most-recent-edit-wins" uses repo-HEAD commit time, not per-file time, so unrelated commits made it look newer than Emma's wiki edit and it clobbered. (Possible future fix: per-file timestamp comparison, or a hard wiki-wins guard for this page.)

== Notes ==

''(free-form scratch space for Emma to leave context that doesn't fit a specific question)''

== Sync-policy exception note ==

Conflict resolution for <code>git_synced/</code> (and the other sync dirs) is '''most-recent-edit-wins''' as of 2026-05-30 (whichever side — wiki top revision vs latest git commit — was edited more recently wins; the per-directory static policy is only the tie-break). This page is intended to be edited primarily ON the wiki by Emma. Agents working on <code>shintowiki-scripts</code> should additionally:

# Always pull this page from the wiki at the start of a session (don't trust the repo copy alone).
# When committing changes to other files, never include a stale repo version of this page in a sync push.
# If you're about to edit this file in the repo, first confirm the wiki version hasn't been updated since the last sync — otherwise you'd risk clobbering Emma's answers.

[[Category:Git synced pages]]
