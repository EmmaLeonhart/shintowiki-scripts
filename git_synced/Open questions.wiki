__NOTOC__

== Open questions ==

''(nothing open. Anything you write here gets answered and then deleted — it is a live channel, not an archive.)''

== Wiki-based queue ==

Anything you want done on the wiki side — write it here rather than in the repo.

* ''(empty — add items here)''

== Notes ==

''(your scratch space.)''

== Scheduled items ==

<!-- Both scheduled items below are SETTLED; their bullets are pruned and only the markers
     remain. The marker, not the json, is what stops inject_due_items.py re-injecting an
     item, so it has to outlive the work it guarded. Outcomes live in DEVLOG.md. -->
<!-- scheduled:label-generator-continue-on-error -->
<!-- scheduled:p958-corrections-batch-paste -->

''Injected automatically on each item's due date by <code>scheduled/inject_due_items.py</code>.
Nothing here was visible in <code>queue.md</code> before its due date -- that is the point of it.''

[[Category:Git synced pages]]
<references />
