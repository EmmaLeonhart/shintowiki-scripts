__NOTOC__

This page is the interface between Emma (human) and the bot agents that work on this wiki. Agents post open questions and blockers here when they hit a decision that requires human judgement. Emma answers them on the wiki. Agents read the wiki version each session and act on the answers.

== How to use this page ==

* '''Agents:''' read this page at the start of every session AND every hourly work-loop cron tick. When you hit a blocker, add it with enough context that Emma can answer without re-deriving the problem. '''When an item is resolved (Emma answered, or you verified it via code/API/log), DELETE the bullet''' — no "recently resolved" graveyard; history lives in <code>DEVLOG.md</code> + git log.
* '''Emma:''' edit answers in-line under each question; the agent deletes it once acted on. Free-form context goes under [[#Notes|Notes]].
* '''This page is wiki-wins.''' The wiki is canonical; Emma's edits here propagate down to the repo. (The 2026-06-07 bot clobber of this page is fixed — <code>fetch-depth:0</code> + most-recent-edit-wins + explicit wiki-wins for this page.)

== Open questions ==

''(These mirror actionable blockers in <code>queue.md</code>. Answer inline; the agent deletes each once acted on.)''

* '''CI Wikidata editing broke between 2026-07-02 and 07-06 — the BOT-PASSWORD path lost edit capability (needs Emma).''' <code>direct_daily_edits.py</code> logs in as Immanuelle but every edit now FAILS (run 28802688487 = 0 OK / 299 FAIL: 112× "You do not have the permissions needed", 140× "The save has failed", 47× qualifier fails; my re-trigger 28830335675 = same). Diagnosis: (1) it WORKED on 07-02 (run 28621713230 = 42 "OK: Created"), so this is a regression, not a never-worked. (2) The auth/edit code did NOT change between 07-02 and 07-06 (only ATOMIC_FILES + the edit-cap). (3) The Immanuelle account itself is fine — NOT blocked, has the <code>edit</code> right, editcount 671k; browser QuickStatements still works because it authenticates via OAuth (tag "OAuth CID: 1776"), which carries its own grants. (4) The account has NO <code>bot</code> group and NO <code>writeapi</code> right. So the break is specifically the bot-password (<code>BOT_TOKEN</code>) used by <code>action=login</code> — its grants were revoked/changed or it was invalidated. '''What changed / to check (Emma):''' at <code>Special:BotPasswords</code> for Immanuelle on wikidata.org, confirm the bot password used by <code>BOT_TOKEN</code> still exists and has grants "Edit existing pages" + "Create, edit, and move pages" + "High-volume (bot) editing"; regenerate + update the <code>BOT_TOKEN</code> secret if not. (Emma wants CI to run continuously — NOT QuickStatements — so this is the path; once the token can write again the daily pipeline drains the ~38k backlog.)

== Notes ==

''(free-form scratch space for Emma)''

== Sync-policy exception note ==

Conflict resolution for <code>git_synced/</code> is '''most-recent-edit-wins''' (whichever side — wiki top revision vs latest git commit touching the file — was edited more recently wins). This page is edited primarily ON the wiki by Emma. Agents: always pull this page from the wiki at session start; never push a stale repo copy; before editing it repo-side, confirm the wiki hasn't been updated since the last sync.

[[Category:Git synced pages]]