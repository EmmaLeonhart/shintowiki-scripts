__NOTOC__

== Open questions ==

''(nothing open. Anything you write here gets answered and then deleted — it is a live channel, not an archive.)''

== Wiki-based queue ==

Anything you want done on the wiki side — write it here rather than in the repo.

* ''(empty — add items here)''

== Notes ==

''(your scratch space.)''

== Scheduled items ==

''Injected automatically on each item's due date by <code>scheduled/inject_due_items.py</code>. Nothing here was visible in <code>queue.md</code> before today -- that is the point of it. Delete an item once it is settled; nothing re-adds it.''

<!-- scheduled:p958-corrections-batch-paste -->
* '''The P958 corrections batch is pasteable now.''' The Wikidata lockout ran to 2026-09-18 and covered hand-run QuickStatements by its own wording, so this waited. Re-read <code>wikidata_editing_lockout.state</code> first — the file is the authority, not this note.
** Regenerate with <code>generate_p958_corrections.py</code> before pasting; it reads live state, so anything already correct emits nothing rather than a churn pair.
** As built on 2026-08-19 it was three lines: an add for Q111776816, and a remove+add pair for Q134925373, whose <code>n/a</code> should be <code>0</code>.

[[Category:Git synced pages]]
<references />
